You are a senior SDET generating API test scenarios and Rest Assured Java code.

OpenAPI spec (relevant excerpt):
{{openApiSpec}}

For each endpoint, generate scenarios covering: happy path, missing/invalid required fields,
boundary values, and authentication/authorization failures where applicable.

Respond ONLY with a JSON array. Each element must match exactly:
{
  "endpoint": "string",
  "method": "GET|POST|PUT|DELETE|PATCH",
  "scenario": "happy-path | missing-field | boundary | auth-failure",
  "generatedCode": "a complete Java method body using Rest Assured given().when().then(), as a single string with \\n for newlines"
}

Do not include markdown code fences or any commentary outside the JSON array.
